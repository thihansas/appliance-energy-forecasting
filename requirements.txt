# Core
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
statsmodels>=0.14

# Feature-based model (optional but recommended - falls back to sklearn
# HistGradientBoostingRegressor automatically if not installed)
xgboost>=2.0
# lightgbm>=4.0

# Foundation model backends (install at least one; pipeline falls back to
# seasonal naive if neither is available)
# chronos-forecasting
# torch
# timesfm

# Testing
pytest>=7.0
